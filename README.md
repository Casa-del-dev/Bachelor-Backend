# 🛠 Needed to Reinstall Dependencies

```sh
python -m venv venv  # Create a new virtual environment
.\venv\Scripts\Activate  # Activate it (Windows)
pip install -r requirements.txt  # Install all dependencies
